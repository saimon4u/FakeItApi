apiVersion: v1
kind: PersistentVolume

metadata:
  name: mongo-pv

spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/"
